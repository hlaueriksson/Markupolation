@page "/"
@inject HttpClient Http

<PageTitle>Home</PageTitle>

@((MarkupString)hello)

@code {
    private string hello = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        hello = await Http.GetStringAsync("/api/hello");
    }
}
